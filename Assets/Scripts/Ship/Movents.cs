using UnityEngine;

namespace Assets.Scripts
{
    // Start is called before the first frame update
    public partial class Ship
    {
        public Vector3 StartPosition { get; set; } = new Vector3(-8.50f,-0.50f,00f);

    }
}
